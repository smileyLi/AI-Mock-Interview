# ============================================================
# AI模拟面试系统 - Nginx 配置文件
# 用法：
#   1. 把文件中的 {YOUR_IP} 替换为你的服务器公网 IP
#   2. HTTP 阶段：复制到 /etc/nginx/sites-available/interview
#   3. 获取证书后：取消注释 HTTPS 部分，重新加载 Nginx
# ============================================================

# --- HTTP 阶段（先跑通，然后用 certbot 获取证书）---
server {
    listen 80;
    server_name {YOUR_IP}.nip.io;

    client_max_body_size 20M;

    location / {
        root /opt/ai-interview/frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# --- HTTPS 阶段（certbot 获取证书后取消下面注释）---
# server {
#     listen 443 ssl;
#     server_name {YOUR_IP}.nip.io;
#
#     ssl_certificate     /etc/letsencrypt/live/{YOUR_IP}.nip.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/{YOUR_IP}.nip.io/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     client_max_body_size 20M;
#
#     location / {
#         root /opt/ai-interview/frontend;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:8000/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
#
# # HTTP → HTTPS 重定向
# server {
#     listen 80;
#     server_name {YOUR_IP}.nip.io;
#     return 301 https://$host$request_uri;
# }
